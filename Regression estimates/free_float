function [Output] = free_float(Input,Col)

bin = isnan(Input(:,Col));

Saver = Input(:,Col);

for x=size(Input,1):-1:1
    if bin(x)==1
        Saver(x,1)=Saver(x+1,1);       
    end
end

Output = Saver;

end
